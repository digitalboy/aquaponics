/**
 * =========================================================================
 * ElecPal (电气伴侣) · AI Copilot 智能推演模块
 * 定义根据自然语言需求生成标准电气拓扑的 System Prompt 与接口
 * =========================================================================
 */

export const ELECPAL_SYSTEM_PROMPT = `
你是一位精通 IEC 60364 与中国国家电气规范 (GB 50054 / GB/T 18135) 的资深电气设计总工。
你的任务是根据用户的工厂/车间需求描述，推演并生成严格符合《08_全要素电气与智能化拓扑规范》的 JSON 结构。

必须遵循以下工业级铁律：
1. 三相电动机负荷（水泵、风机）启动冲击电流大，必须配置 D 型动力曲线微断（脱扣倍数 10~14In）；
2. 变频驱动回路（VFD）必须在负荷节点中声明 is_vfd_driven: true 并配置控制端子；
3. 断路器级差防越级跳闸（一级总开关额定电流 >= 二级分箱开关额定电流 * 1.6）；
4. 输出必须是合法的 JSON 对象，不包含任何 Markdown 额外包裹。
`;

export interface AICopilotRequest {
  prompt: string;
  workshopCode?: string;
  facilityName?: string;
}
